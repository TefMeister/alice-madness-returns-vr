# Research index

**Last `/gr` pass: 2026-09-08 (estate sweep) — CHECK-IN, inbox drain.** **Inbox drained: 1 file**, the modding lane's verdict on all six 2026-09-07 drops. ✅ Two topics move 🆕→✅. The `[inferred-static]` mouse-path call is now **measured** from the import table (Win32 cursor/message path, no Raw Input) `[verified-numerically 2026-09-08]`, so route 4 is live. ⭐⭐ **The most valuable item is a correction to this lane and to `/sr`:** the broken ViGEm bus does **not** block the pad route here — Alice imports `XINPUT1_3.dll` **by ordinal**, so an in-process `xinput1_3.dll` proxy needs no bus, no driver and no virtual device, and `prince-of-persia-2008-vr`'s existing proxy is a superset of what Alice needs. Pad route re-tagged **blocked → available, mechanism untested**; whether Alice ever *polls* XInput is the real unknown, and POP ships an entry-counter that would port. A `Supersedes:` drop has gone to `flat-to-vr-cross-engine-research/inbox/` because `/sr`'s device-state-injector material carries the same too-broad blocker. ❌ One path correction accepted: the exec file lives in `Binaries\Win32\`, not `Binaries`. Nothing searched on the web this pass — the drop is all first-party measurement, and the three remaining `[reported]` items are answered by one launch, not by research.

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep, THIRD pass today) — CHECK-IN, corrective** (board `OPEN` block re-read after the `/lm enslaved` session's cross-project correction; no search)**.** Inbox empty. **⚠️ I withdrew one of my own drops from this morning, in full.** My `2026-09-07b` drop asked the modding lane to record that Alice *"has two config trees, and which one is live differs per machine"*. **That is wrong, and dossier §10 was right all along.** "Monkey" is **Ninja Theory's codename for *Enslaved: Odyssey to the West*** — `My Games\UnrealEngine3\MonkeyGame\` is **Enslaved's** config tree, its `MonkeyEngine.ini` carrying `EditPackages=NTEngine` / `NTGameFramework` with **zero** Alice references `[verified-numerically 2026-09-07]`. There are not two *Alice* trees; there is one (`AliceGame`) plus a second game's that happens to be installed on the same machines. **How it propagated is the useful part:** a 2026-09-05 session mis-attributed the script package, a 2026-09-07 session *partially* corrected it to "two trees, varies per machine" while keeping the false premise, and **I read that most-recent correction as settled and filed it onward without checking its premise**. A correction is not automatically the truth. Withdrawal filed as `2026-09-07d`, carrying a `Supersedes:` on my own file and asking for the text to be reverted if already folded in. 🚨 **And a second hazard landed while this pass ran, against a route I recommended:** the **dev PC's ViGEm bus is broken** (`ROOT\SYSTEM\0004` in an Error state, present before any pad was created) `[measured 2026-09-07]`. **Alice's automation runs on the dev PC**, so `virtual-pad.py` — route 3 in my `2026-09-07c` drop — is currently expected to fail there **for a reason that has nothing to do with Alice**, and a session trying it would record a false "Alice ignores XInput". Filed as `2026-09-07e`. It has already cost the sibling project one wrong conclusion, since downgraded. The technique is unaffected — the sibling's original result was on the **home** PC — and the console `exec` route, being keyboard-only, is untouched and remains the recommended first move.

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep, SECOND pass today) — FULL** (board `OPEN` block re-read after the same day's `/lm` result and gate drop, INDEX, dossier §10, four sibling control profiles)**.** Inbox empty. **The board moved between my two passes today** — both eyes are confirmed live (the rock scales with ipd at R² = 0.99948) and **the gate dropped FLAT → PD**, adding a `[PD]` row: give the harness mouse injection, because *"camera control — the one automation capability still unproven on this game"*. My earlier CHECK-IN stamp predates that and is superseded. **One new topic, and it de-risks the row before it is built:** `MOUSEEVENTF_MOVE` **already drives the camera on `enslaved-vr`** — 32-bit UE3 on D3D9, the same engine generation, bitness and render API — at ~120 steps of `dx=40` for a large arc. ⭐ **And a second route needs no mouse and is already written**: `virtual-pad.py` (ViGEmBus) hot-plugs a synthetic X360 pad into a *running* game; proven on the same UE3 sibling (right stick swung the camera, frame delta 62.5 against an idle baseline of ~2) and called *"the best in-game route"* by `doom-2016-vr` because XInput binds **regardless of focus rules or DirectInput exclusive mode**. ⚠️ Balanced by the counter-example that stops this being a blanket rule: on `psychonauts-vr` relative mouse movement does **not** rotate the camera, disproved with a control that could have produced a positive. So the recommendation is **both routes, judged against a no-input control on a large arc** — and `virtual-pad.py` first, because it is a run rather than a build. Also carried across: a frame-delta trap the sibling already paid for (animated scenery gave a no-input control *above* the signal from a working keypress), which matters in rainy, foggy Whitechapel. ⭐ **Then a second, better answer arrived and revised my own recommendation:** UE3 binds a **keyboard key straight to an analog axis** (`Axis aTurn … AbsoluteAxis=100`, confirmed unprompted from two independent retail/UDK config files — and **Alice's own `AliceInput.ini` already uses that grammar**), and **`exec <file>` turns one keypress into a full Python→console channel**, reaching **`BugItGo X Y Z Pitch Yaw Roll`** — absolute position *and rotation*, with `BugIt` as a read-back. So aim can be **absolute and self-verifying** instead of a calibrated nudge, and the mouse leaves the critical path. Revised order: **console `exec` → keyboard `Axis` → `virtual-pad.py` → `MOUSEEVENTF_MOVE`**. ✅ The mouse would probably have worked anyway — Alice looks to be on the **Win32 cursor/message path, not Raw Input** — but ⚠️ **I had to correct my own earlier drop**: the sibling's "120 steps of `dx=40`" is **not portable between machines**, because Windows pointer ballistics can scale an injected delta by up to 4×. **Three pointers filed to `engine-research/inbox/` — deliberately one ask each**, after `/gs` found this morning that bundled drops get their cheap half done and their expensive half forgotten; the third carries a `Supersedes:` on my own ordering. One engine-agnostic half (DirectInput on Vista+ is a Raw Input wrapper, so "DirectInput ignores injected input" is a pre-Vista folk memory) went to the cross-engine library for `/sr`._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep) — CHECK-IN** (board `OPEN` block + INDEX; nothing searched)**.** Inbox empty. **Nothing new.** Board unchanged since 2026-09-05: one `[FLAT]` launch answers three things and the first needs no keypress. Nothing researchable is blocking it._

_Previous: **Last `/gr` pass: 2026-09-05 (estate sweep) — CHECK-IN** (board OPEN block + INDEX; nothing searched)**.** Inbox empty. **Nothing new, and not searched** — the single row is `[FLAT]`: the two-eye build is now deployed on the **home PC** as well (built there to exactly the recorded 702,976 B), and its first check needs no keypress at all. ⚠️ Carried from the board because it cost time once already: the ini is **`MonkeyEngine.ini`**, not `AliceEngine.ini` — UE3 names it after the script package `[verified-numerically 2026-09-05]`. 🔗 **Cross-project, for later:** this is a UE3 game, and today's `enslaved-vr` finding — that a retail UE3 build shipping with `DO_CHECK` on turns every `check()` into a stringified expression naming the globals it guards — is a static-recon technique that transfers here directly if this project ever needs to locate an engine global. Filed there and in the cross-engine library; deliberately not duplicated as a topic here._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep) — CHECK-IN** (board OPEN block + INDEX)**.** Inbox empty. **Nothing new, and not searched** — both rows are `[FLAT]` (launch the new proxy; the `Stereo3D` row is answered statically) and the 2026-09-03 FULL pass already closed the one research target the modding side offered._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep, second pass today) — FULL** (inbox drained — three `/pd`+modding drops; INDEX; the three topics they answer re-read; one targeted search)**.** Drained the modding side's three verdicts: **SteamStub confirmed** by the `0xC0DEC0DF` header magic (⚠️ the `steam_api.dll` import witness I suggested is *absent* here and is positive-only — recorded so it is never read as a negative); **the Direct-vs-Automatic scan ran on the unpacked copy and returned AUTOMATIC**, the pre-committed branch, with the still-packed copy as a side-by-side negative control; and the `Stereo3D` menu row is wired `ExecStereo3D → ExecConfigData → EnableStereo3D` with `AllowNvidiaStereo3d` already `True` — **do not research either**. One new topic answering the one target the modding side did offer: **3D Vision is a discontinued driver feature (last driver 425.31, April 2019)**, so the game's own stereo row will not light on a current driver without a downgrade or a driver-modding tool; DX9 is *reported* to have survived the 2020 DX11 removal, unconfirmed. Framed explicitly as not a VR route. Pointer row in `alan-wake-vr`; platform half to the cross-engine inbox._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep) — FULL** (inbox drained, INDEX and the three 2026-09-0x topics re-read, board read)**.** Drained the modding side's verdict on the NVAPI scan and answered the question it closed with: **the wrapper is SteamStub** — a `.bind` section plus an entry point outside `.text` is its published signature, `.te…_
_Previous: **Last `/gr` pass: 2026-09-02 (scoped run, fourth pass) — FULL** (dossier read in full, all ten topics re-read, board and the three 2026-09-01 modding notes read)**.** Inbox empty. One new topic on the question every note calls the crux: **Direct vs Automatic …_
_2026-09-01 — CHECK-IN:_ inbox empty. One new topic closing the modding side's same-day open
question: `NvStereoFixTexture` is NVIDIA's own `StereoParmsTexture`, with separation in `.r`,
convergence in `.g`, and an explicit eye sign (−1 left / +1 right) in `.b`. No shader disassembly
needed. Combined with `ViewProjectionMatrix` at `c0`, the stereo plan for this build was fully
specified without a launch. One real tension recorded rather than smoothed over (Epic calls the
integration "3D Vision *Direct*"; the eye-sign channel is the *Automatic* correction signature) —
the plan works either way.

Every research topic gathered for this project, newest first. Each row links to a self-contained
write-up in `topics/`. Status tags:

- 🆕 **new** — found, not yet acted on by the modding side.
- 👀 **reviewed** — a modding session has read it and factored it into a decision, but nothing shipped from it yet.
- ✅ **incorporated** — directly led to a real change (code, a test, a note) in one of the other five repos; linked below.
- ❌ **dead end** — checked out, didn't pan out; kept for the record so it isn't re-investigated from scratch.

| Date | Topic | Status | Summary |
| --- | --- | --- | --- |
| 2026-09-07 | [The mouse may be the wrong question: `exec <file>` is a full Python→console channel](topics/2026-09-07b-the-console-exec-file-is-a-full-python-to-game-channel-over-keyboard-alone.md) | ✅ incorporated | **✅ VERDICT 2026-09-08 (`/pd`): adopted, with one path corrected.** ❌ **The exec file's directory given here was wrong:** this topic said `…\Alice Madness Returns\Binaries`. **The exe lives in `Binaries\Win32\`**, which is its working directory, and UE3 builds are also documented reading exec files from `<Game>\Config`. Rather than pick one and risk a **silent no-op that cannot be told apart from "no cheat manager"**, the harness now writes the same file to **all four** candidates — all four exist on that machine — and prints which it managed. The finding itself is the valuable part and is unaffected; only the path was wrong. ✅ The 2026-09-07d **withdrawal was checked rather than trusted**, and is correct: `My Games\UnrealEngine3\MonkeyGame\Config\` carries `EXEName=MonkeyGame.exe`, `MKAIPathbuildingScout`, `ScriptPaths=..\..\MonkeyGame\Script` and **zero Alice references anywhere in the tree** `[verified-numerically 2026-09-08]` — it is Enslaved's. §10's `AliceGame\Config\AliceEngine.ini` stands. ⭐ **The "read the whole inbox before draining any of it" rule earned its place here**: draining oldest-first would have written the retracted "two config trees" claim into the dossier and only then met its withdrawal. 🐛 And the cosmetic §10 typo had a better cause than a missing `\A`: the file held a literal **BEL byte (0x07)** — `\a` consumed as an escape when the line was written. Worth knowing generally: **a path in our docs that looks like it lost a character may have had it interpreted instead.** | ⭐⭐ **Revises my own earlier topic's recommended order** — two routes are cheaper *and more precise* than any input injection. **(1) `exec <file>`:** bind a key to `exec commands`, put an extensionless `commands` file in `…\Binaries`, and **rewrite it from Python while the game runs** — the next press executes the new contents `[reported 2026-09-07]`. One synthetic keypress, which the harness already drives, becomes **any console command**. That reaches **`BugItGo <X> <Y> <Z> <Pitch> <Yaw> <Roll>`**, a UE3 `CheatManager` exec setting player location **and rotation** absolutely, with **`BugIt`** printing the current pair — so aim becomes **absolute, repeatable and self-verifying**, and "did the camera move?" becomes a number instead of a screenshot judgement (which matters given the sibling's frame-delta hazard in rainy Whitechapel). Also `playersonly`, `toggledebugcamera`, `fov`, `ghost`, `fly`, `teleport`. ⚠️ `SetMouseSensitivity` and `SetRotation` **do not exist**; the real names are `SetSensitivity` and `BugItGo`. Enable with `-freeconsole -allowcheats` + a `Tilde` console binding. **(2) Keyboard key → analog axis:** `Bindings=(Name="TurnLeft",Command="Axis aBaseX Speed=-200.0 AbsoluteAxis=100")` — confirmed **unprompted and identically from two independent files** (a retail UE3 `BmInput.ini` and UDK's stock `UDKInput.ini`, which also gives `Axis aLookUp` for pitch), and **Alice's own `AliceInput.ini` already carries the grammar** (`Axis aTurn Speed=-1.0 DeadZone=0.2`), so `aTurn`/`aLookup` are live axes here. `AbsoluteAxis=100` is what makes a digital key hold a constant axis value `[inferred-static]`. ⚠️ Add the binding to **both** `DefaultInput.ini` (with `+`) and the `My Games` `AliceInput.ini` (without) or the game strips it — and which `My Games` tree is live differs per machine. ⚠️ A held turn key will likely **accelerate**, because `AAlicePlayerController` ramps look speed via `aTurnElapsedTime`/`aLookUpElapsedTime`. ✅ **The mouse itself would probably have worked:** Alice looks to be on the **Win32 cursor/window-message path, not Raw Input** `[inferred-static]` — MadnessPatch hooks `UpdateMouseLock` (calling **`ClipCursor`**) and **`ProcessDeferredMessage`**, and a Raw-Input reader has no reason to clip the cursor. ⚠️ Its `dinput8.dll` proxy is **not** evidence about the mouse — UE3 uses DirectInput8 for gamepad enumeration. ⚠️ **Two caveats that apply whatever route wins:** **UIPI makes `SendInput` fail silently** across an integrity boundary (neither the return value nor `GetLastError` says so), and **Windows pointer ballistics scale injected deltas by up to 4×** — so the sibling's `dx=40` figure is **not portable**. **Also:** Wemino's **MadnessPatch** (GPL-2.0) fixes **stuck keys from a key-state desync** and disables the game's heavy mouse smoothing/negative acceleration — a game with that bug is exactly the kind that mishandles rapid synthetic keys, so it is the first suspect if the harness ever sees a sticking key. And the built-in `FreeCamera` exec exists but guides report **"the mouse doesn't move the camera"** there, recommending a controller — a second argument for the virtual-pad route. ⚠️ **Recorded as unresolved:** whether `SendInput` reaches a pure Raw Input consumer is genuinely contested in public sources; it does not need resolving for Alice. **One launch settles the row's scope:** `-freeconsole -allowcheats`, open the console, type `BugIt`. |
| 2026-09-07 | [The camera-injection question is already answered on our UE3 sibling](topics/2026-09-07-the-camera-injection-question-is-already-answered-on-the-sibling-ue3-game.md) | ✅ incorporated | **✅ VERDICT 2026-09-08 (`/pd`, dev PC, no launch) — all six 09-07 drops drained, and the asks were followed:** the two keyboard-only routes are recorded in dossier §10 **before** the mouse code, and the harness implements route 1 first. ✅ **Upgraded from inference to measurement:** the `[inferred-static]` reading that Alice's mouse path is the **Win32 cursor/message path, not Raw Input** — inferred here from MadnessPatch hooking `UpdateMouseLock` — is now read straight off the import table of `AliceMadnessReturns.exe` `[verified-numerically 2026-09-08]`: `ClipCursor`, `GetClipCursor`, `GetCursorPos`, `SetCursorPos`, `GetKeyState`, `GetMessageW`, `PeekMessageW` and `DINPUT8.dll`→`DirectInput8Create` present; `RegisterRawInputDevices`, `GetRawInputData`, `GetAsyncKeyState`, `GetKeyboardState` **absent**. The conclusion now rests on our own read rather than on someone else's patch, and **route 4 is worth trying on this game**. ⭐⭐ **CORRECTION that matters most — the ViGEm blocker does NOT apply to the pad route here.** This topic (and `/sr`'s device-state-injector drop) treated the virtual-pad route as blocked because the dev PC's **ViGEm bus** is in an Error state. True of a ViGEm *virtual device*, and irrelevant to this game: **Alice imports `XINPUT1_3.dll` by ordinal 2 and 3** `[verified-numerically 2026-09-08]`, exactly the shape `prince-of-persia-2008-vr` has, so an `xinput1_3.dll` **proxy fabricates a pad inside the process** — no bus, no driver, no virtual device. That sibling's proxy already exists, loads, and pins its ordinals in a `.def` (it needs ordinal 4 as well; Alice does not import it, so POP's `.def` is a superset). **Re-tagged: pad route is AVAILABLE, mechanism untested.** ⚠️ Genuinely unestablished: whether Alice ever *polls* XInput — POP's proxy loaded fine and that game never called `XInputGetState` once, and POP's 2026-09-08 build carries an entry-counter instrument that answers exactly that and would port with the proxy. ✅ Both silent-failure traps flagged here are handled: pointer ballistics are now **measured** on that machine (thresholds (6,10), acceleration **ON**, speed 6/20) `[measured 2026-09-08]` — so the warning is live, an injected delta may be scaled, and a step size calibrated there would not port; and the UIPI trap is answered by the `mouse` command stating outright that `SendInput` returning success does not mean the game saw it, with `bugit` as the read-back. ⏳ Still `[reported]`, untested: `BugIt`/`BugItGo` existing in this **retail** build, the `Axis aTurn` binding of route 2, and the `-freeconsole -allowcheats` requirement — **one launch tests all three at once**, and that is now the project's top `[FLAT]` row. **Aimed at the `[PD]` row added when the gate dropped to PD today** — *"give `alice_harness.py` mouse injection … camera control is the one automation capability still unproven on this game"*. ✅ **`SendInput MOUSEEVENTF_MOVE` already drives the camera on `enslaved-vr`** — **32-bit UE3 on D3D9**, the same engine generation, bitness and render API as Alice: *"about 120 steps of `dx=40` swung the view through a large arc onto entirely different scenery"* `[verified-live, per its control profile]`. The row is not speculative; copy the working parameters instead of guessing a scale. ⭐ **A second route needs no mouse and is already written:** `flat-to-vr-RE-toolkit/tools/virtual-pad.py` (ViGEmBus + vgamepad) hot-plugs a synthetic X360 pad into a **running** game — proven on the same UE3 sibling `[verified-live 2026-09-03]` (right stick 2.0 s swung the camera onto different scenery, frame delta 62.5 vs an idle baseline of ~2; left stick moved the character; no restart), and independently by `doom-2016-vr` `[verified-live 2026-09-04, n=2 per axis with reversal]`, which calls it *"the best in-game route: XInput is imported directly, so the game binds the virtual pad as a real Xbox 360 controller **regardless of focus rules or DirectInput exclusive mode**"* — against the contrasting hazard that *"sendinput follows focus"*. Alice is a console-era third-person game with native pad support, so a right stick is the input its camera was designed around, and **this is a run, not a build**. ⚠️ **The counter-example that stops this being a blanket rule:** on `psychonauts-vr` plain relative mouse movement does **not** rotate the camera — disproved against both synthetic DirectInput deltas and **real `SendInput MOUSEEVENTF_MOVE` with the foreground grab verified**, with a control (arrow-key walking swung the camera hard) proving the field was live and reachable. Input handling is per-game; **carry that control or a null result means nothing.** ⚠️ **And a measurement trap the sibling already paid for:** animated grass and water gave a no-input control of 3.4–23.3, *above* the signal from a working keypress, and scored a key that demonstrably walks the character as "no effect" — Whitechapel has rain, fog and animated set dressing, so judge on a **large arc read from two screenshots**, never a small frame delta. Recommendation: **both routes, `virtual-pad.py` first**, then record the verdict in the game's control profile, whose camera entry still reads "unexercised". Also carried: `alan-wake-vr` found scancodes did **not** reach it while virtual-key events did — the reverse of `enslaved-vr`'s advice, `n=1` each way, so try one and fall back. |
| 2026-09-03 | [3D Vision Automatic on a current driver: what it takes, and why it is not a VR route](topics/2026-09-03b-3d-vision-automatic-on-a-current-driver-what-it-takes-and-why-it-is-not-a-vr-route.md) | 🆕 new | Answers the modding side's one offered target. **3D Vision is a discontinued driver feature: NVIDIA announced the end on 2019-04-11 and 425.31 is the last driver that includes it** `[reported]`; DX11 stereo survived to 452.06 via workarounds and was removed with the RTX 30-series driver (Oct 2020); **DX9 games are *reported* to remain compatible on current drivers** (one source, unconfirmed — the claim that matters most for Alice). Display side: 120 Hz 3D Vision display + emitter, 3DTV Play, or **Discover-mode anaglyph glasses on any card**. geo-11 (2022) replaces the driver for DX11 only; DX9 reaches it via dgVoodoo2. ⇒ On this machine the `Stereo3D` row will not light without a driver downgrade or a driver-modding tool — *unless* the DX9 claim holds, in which case anaglyph glasses are the cheapest reference picture of the developers' own stereo. ⚠️ Not a VR route; the plan does not depend on it. |
| 2026-09-03 | [The `.bind` section is SteamStub, and a public unpacker restores `.text`](topics/2026-09-03-the-bind-section-is-steamstub-and-a-public-unpacker-restores-text.md) | ✅ incorporated | **✅ 2026-09-03: CONFIRMED by the SteamStub `0xC0DEC0DF` header magic at `.bind+0x4A0` (Steamless: Variant 3.1 x86); a copy was unpacked and the scan ran the same day. ⚠️ The `steam_api.dll` import witness suggested below is ABSENT here — the game has no Steamworks API at all — so it is a positive-only witness, never a negative.** **⭐ Unblocked the Direct-vs-Automatic scan.** Answers the drop's closing question: the wrapper whose `.bind` section, out-of-`.text` entry point and 8.00-entropy code section stopped the scan is **Steam's own DRM stub**. `.text` is encrypted at rest and decrypted into memory at startup, which is exactly why the `NvAPI_Initialize` positive control failed. Two open-source unpackers cover 32-bit SteamStub 1–3 ([Steamless](https://github.com/atom0s/Steamless), [Steamstub-v3-Unpacker](https://github.com/GHFear/Steamstub-v3-Unpacker)), so the task returns from `[FLAT]` to `[PD]`: unpack a **copy**, re-run the control first, then read the discriminators. Two cautions: an unpacked exe generally will not launch standalone (expected, not a failed unpack), and the runtime-dump route stays the fallback. Second witness to confirm: `steam_api.dll` in the import table. |
| 2026-09-02 | [Direct or Automatic is a one-scan static question — Alan Wake's NVAPI caller count transfers to Alice's exe](topics/2026-09-02-direct-or-automatic-is-a-one-scan-static-question-alan-wakes-caller-count-transfers.md) | ✅ incorporated | **✅ Re-run 2026-09-03 on the unpacked copy: AUTOMATIC.** `Initialize` control = 1; `SetActiveEye` = 0; `SetDriverMode` = 0; `Activate` + three getters = 1 each, all in one 825-byte function; the still-packed copy returned 0 for everything *including the control*. The pre-committed Automatic branch held; the method now has two consistent applications (Alan Wake, Alice). **Scan run 2026-09-02; result was BLOCKED, not negative.** All three ids returned zero occurrences in `.text` — including the `NvAPI_Initialize` positive control — because the section is encrypted at rest (entropy 8.00, no disassembly, no `CC` padding, entry point in `.bind`). The control did its job: without it this would have read as a clean "Automatic". The method is sound and is re-armed by the 2026-09-03 SteamStub topic above. ⚠️ Do **not** read the 105-entry NVAPI table at `013d452c` as evidence for Automatic — it is the linked SDK's fixed table (it carries `VIO`/`Mosaic`/`I2C` and omits `NvAPI_Initialize` itself). ⭐ Direct mode requires `SetDriverMode(DIRECT)` before device creation and `SetActiveEye` per eye; Automatic needs neither. NVAPI dispatches by id, so count direct callers of the wrappers for **`0x96EEA9F8` (`SetActiveEye`, Direct-only)** and `0x5E8F0BEC` in `AliceMadnessReturns.exe`, with `Initialize 0x0150E828` as the positive control — the method that retired Alan Wake's shortcut on 2026-09-01. Callers ⇒ the engine already renders two eyes and the per-eye `c0` write sits between the LEFT and RIGHT calls; none ⇒ Automatic, plan unchanged. Players: the `Stereo3D` toggle turns the emitter on, drops fps and shifts shadows into stereo. MadnessPatch 3.0.0+ adds `EnableConsole` on F2 — F2 is patch-only, stock is Tilde. |
| 2026-09-02 | [`NvStereoFixTexture`'s exact format, and who actually writes it](topics/2026-09-02-nvstereofixtexture-exact-format-and-who-writes-it.md) | 🆕 new | Closes the one gap the 2026-09-01 topic left: `StereoTexWidth`/`Height`/`Format` are **8×1, `D3DFMT_A32B32G32R32F`**, straight from NVIDIA's `nvstereo.h`. More importantly, the header shows the **application populates the texture itself each frame via NVAPI queries** — the driver only reads it — so a proxy intercepting those calls or the texture write controls every one of the 14,479 sampling shaders without fighting the driver for the resource. |
| 2026-09-01 | [`NvStereoFixTexture`'s layout is documented — no disassembly needed](topics/2026-09-01-nvstereofixtexture-layout-is-documented-no-disassembly-needed.md) | 🆕 new | ⭐ Answers the 2026-09-01 open question from a **first-party source**: the texture is NVIDIA's `StereoParmsTexture` from `nvstereo.h`, holding *"eye-specific separation"* (`.r`), *"convergence"* (`.g`) and a *"unit vector identifying the current eye"*, **left = −1, right = +1** (`.b`); it is **app-provided** and updated once per frame at frame start, "even while the device is lost". A proxy that binds its own texture drives all 14,479 sampling shaders with no driver, no 3D Vision and no shader patching. Also: UE3 stereo is **fullscreen-only and does not work in the editor** — a real constraint on any live test. |
| 2026-08-25 | [enslaved-vr sibling project: live UE3/D3D9 findings](topics/2026-08-25-enslaved-vr-sibling-project-ue3-d3d9-live-findings.md) | 👀 reviewed | This portfolio's own enslaved-vr project already has a live-captured UE3-on-D3D9 constant-register analysis, a proven d3d9.dll proxy blueprint (exact vtable slots), and confirmation UE3's default console key is Tilde — directly refines expectations for §4/§6/§7/§9. Factored into ENGINE-DOSSIER.md §3/§6. |
| 2026-08-25 | [Cross-engine library: D3D9 generic-driver notes](topics/2026-08-25-cross-engine-library-d3d9-generic-driver-notes.md) | 👀 reviewed | This portfolio's own cross-engine library explains why vorpX's Geometry 3D suits D3D9 so well, and documents a dgVoodoo2→geo-11+shader-fix backup path Alice is well-positioned for given its existing HelixMod fix. Factored into ENGINE-DOSSIER.md §12. |
| 2026-08-25 | [Native stereo3D mostly works, only UI is broken](topics/2026-08-25-native-stereo3d-mostly-works-only-ui-broken.md) | 👀 reviewed | **Addendum 2026-09-03: the menu's `Stereo3D` row is wired `ExecStereo3D → ExecConfigData → native EnableStereo3D`, and the `AllowNvidiaStereo3d` gate is already `True` (an NVIDIA-supplied UE3 patch, per the `NVCHANGE` markers) — settled statically by the modding side, do not research.** HelixMod's fix reveals the game ships its OWN built-in stereoscopic-3D camera system that's already correct except for flat UI depth — potentially means the per-eye projection work is already solved by the developers, not something to reverse-engineer from scratch. Factored into ENGINE-DOSSIER.md §6/§12. |
| 2026-08-25 | [UE3 public camera architecture documentation](topics/2026-08-25-ue3-public-camera-architecture-documentation.md) | 👀 reviewed | Unlike every other proprietary-engine front in this portfolio, UE3's camera system is publicly documented end-to-end: PlayerController/UpdateViewTarget at the gameplay layer, and the view-projection matrix in shader constant register c0 (with a PreViewTranslation split) at the shader layer. Factored into ENGINE-DOSSIER.md §6. |
| 2026-08-25 | [vorpX Geometry 3D + motion controllers — strongest precedent](topics/2026-08-25-vorpx-geometry3d-motion-controllers-strongest-precedent.md) | 👀 reviewed | vorpX already delivers true Geometry 3D stereo AND working motion-controller emulation for this exact game — the best VR-feasibility signal found anywhere in this portfolio so far. Factored into ENGINE-DOSSIER.md §12. |
| 2026-08-25 | [MadnessPatch — comprehensive prior art](topics/2026-08-25-madnesspatch-comprehensive-prior-art.md) | 👀 reviewed | An open-source community patch by a known modder (EchoPatch/MarkerPatch) already exposes the dev console (F2), disables camera smoothing (VR-critical), fixes framerate-dependent physics, and confirms PhysX — touches nearly every open dossier section at once. Factored into ENGINE-DOSSIER.md §3/§6/§8/§9/§12. |
| 2026-08-25 | [Native FOV console command confirmed](topics/2026-08-25-native-fov-console-command-confirmed.md) | 👀 reviewed | `FOV <10-150>` is a real, config-bindable native console command (via BaseInput.ini), confirmed by independent Nexus ultrawide mods — a low-risk starting point for early camera probing. Factored into ENGINE-DOSSIER.md §9. |
| 2026-08-25 | [DRM history: EA Cuckoo removed 2022](topics/2026-08-25-drm-history-ea-cuckoo-removed-2022.md) | ✅ incorporated | EA's "Cuckoo" authentication DRM (tied to the 2016 delisting saga) was removed from the relisted Steam build via a January 2022 patch — explains why our own M0 static recon found no DRM. Factored into ENGINE-DOSSIER.md §4. |

## How to add a topic

1. New file in `topics/`, named `YYYY-MM-DD-short-slug.md`.
2. One row added to the table above, newest at the top.
3. Update the status tag here as it moves through review → incorporated/dead-end (the modding side should update this when it acts on a lead, so the index reflects reality without the research side needing to poll).
